import React from 'react'

import addIcon from '../images/add.svg';

const AddTask = () => {
    return (
        <div className="add-task pointer">
            <img className="w-25" src={addIcon} />
        </div>
    )
}

export default AddTask
